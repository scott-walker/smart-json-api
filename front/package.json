{
  "name": "docker-starter-front",
  "version": "1.0.0",
  "description": "Lightweight Node.js Frontend for Docker Starter",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && cp -rp src dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["front", "nodejs", "docker"],
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
